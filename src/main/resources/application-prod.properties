#################################
## spring.profiles.active=prod ##
#################################

server.port=8080
spring.application.name=kanban-board
version=1.0.1

#create this database with this command:
# create database project_management;
spring.datasource.url=jdbc:mysql://localhost:3306/kanban_board?useSSL=false
spring.datasource.username=root
spring.datasource.password=P@55w0rd

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.datasource.initialization=never
spring.jpa.show-sql=true

spring.thymeleaf.cache=true
